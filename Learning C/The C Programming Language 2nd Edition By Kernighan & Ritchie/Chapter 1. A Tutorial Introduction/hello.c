#include <stdio.h> /*this is a comment*/ //but this is also a comment

int main() { //declares the function main, needed for any program to perform their magic
  printf("hello, world\n"); //it do be printin tho
  return 0; //returns a value, necessary at the end of all functions, with the exception of main (the same result is got by ommitting it)
}           //in the case of main, it returns exit code 0, meaning success
